{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Stock Analyzer (venv)",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/stockanalyzer.py",
            "console": "integratedTerminal",
            "python": "${workspaceFolder}/venv/Scripts/python.exe",
            "cwd": "${workspaceFolder}"
        }
    ]
}
